// import * as dotenv from 'dotenv';

// dotenv.config();
// if(import.meta.env.MODE === "development"){
//     //use dev keys
//     }
//     else{
//     //use .env variables
//     }

const HOST = import.meta.env.VITE_HOST;

export default HOST;